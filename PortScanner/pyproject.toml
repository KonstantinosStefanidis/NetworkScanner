[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "networkscanner"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["scapy", "tqdm"]

[project.scripts]
netscan = "scanner.cli:run"
